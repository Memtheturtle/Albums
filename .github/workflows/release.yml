name: Build mod zip and release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create mod zip
        run: |
          mkdir -p build/"Albums"
          shopt -s extglob
          cp -R !(build|.git|.github) build/"Albums"/
          cd build
          zip -r "Albums-${GITHUB_REF_NAME}.zip" "Albums"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: build/"Albums-${GITHUB_REF_NAME}.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

